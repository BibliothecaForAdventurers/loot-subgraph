{
    "name": "@bibliothecaforadventurers/subgraphs",
    "private": true,
    "workspaces": [
        "packages/*",
        "subgraphs/*",
        "subgraphs/**/*"
    ],
    "scripts": {
        "build": "yarn workspaces run build",
        "codegen": "yarn workspaces run codegen",
        "prepare:mainnet": "yarn workspaces run prepare:mainnet",
        "prepare:rinkeby": "yarn workspaces run prepare:rinkeby",
        "prepare:arbitrum": "yarn workspaces run prepare:arbitrum",
        "prepare:arbitrum-rinkeby": "yarn workspaces run prepare:arbitrum-rinkeby"
    },
    "devDependencies": {
        "@graphprotocol/graph-cli": "^0.22.1",
        "@graphprotocol/graph-ts": "^0.22.1",
        "mustache": "^4.2.0"
    }
}